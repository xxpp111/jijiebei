# design/ — Claude Design 设计稿区

Claude Design 产出的设计稿放这里，**受 git 管**（开发模式见记忆 `dev-workflow-claude-design-to-opus`）。

## 🎨 渲染架构（设计前必读）
> 飞书画板（Claude visualizer 风格）：<https://bytedance.larkoffice.com/docx/KMIodWA5Wo9L6exPkXYc6D4AnJh>

实测 `danshua.fire`：每屏 = **4 层节点叠出来**，决定了各工具分工——
- **① 静态美术整图**（`images/bg/`）：背景1.jpg 1280×720、回归杯横幅.png（待换）、背景2/群号/mask → **GPT-image-2 出无字整图**
- **② 控件「占位」视觉**：按钮/槽位/面板底全是 `singleColor` 纯色块拉伸 + 默认 Cocos 按钮/EditBox + lock 图标 → **几乎无 UI 美术，正好整套重做**
- **③ 动态游戏图标**：cmd/map 空占位，运行时 URLSprite 加载 指挥官60×60/地图200×70/因子图标 → **既有素材不动，设计围着固定尺寸排**
- **④ 文字 Label**：标题/模式名/选手/胜负；mode/players/result 框仅 103×50 会溢出 → 定字号层级 + overflow

**分工结论**：**Claude Design = 最大块**（②控件 + ④文字 的整套样式/布局栅格/配色/字号层级 + 组件&data.js 结构）；GPT-image-2 出①整图；③图标不动；我按稿在 Cocos 组装。

## 约定
- 每一版放一个子目录：`design/v1/`、`design/v2/` …（把 Claude Design 导出的 `HTML/CSS/React/data.js` 解压进去）。
- 改稿流程：Claude Design 改 → 导出 → 替换对应版本目录 → `git commit` → 我 `git diff` + 看截图 → 改 Cocos。
- 每版配套截图放 `design/vN/shots/`。
- ⚠️ 设计稿是 web（HTML/CSS/React）；本项目落地是 **Cocos Creator（节点+Label+Sprite）**。设计稿＝视觉/布局/数据**规范参照**，由我翻译成 Cocos，不直接进项目。

## ✅ 已锁定设计决策（2026-06-08，yb 确认）
1. **赛事名：集结杯**（弃用「回归杯」旧名，UI 不得再出现回归杯）
2. **联名 logo：** CM 金属 dM/CM 字标 + 赛事名「集结杯」**左右并排、加分隔线**
3. **三版方向按「质感」拉开**（共用 深藏青 navy + 金属金 + 银 基调）：A 精修金属 / B 极简 HUD / C 重描边漫画感
4. **整屏背景：星际2 战场实景照（暗化处理）**；背景不带字，文字用 Cocos Label 叠
5. **首页模式按钮：弹性栅格**，mock 先放核心 6 个，最终 6–9 个待土豆拍（⚠️ 9 vs 6 待最终确认）
6. **指挥官分组：** 6 真头像 + 占位补满到 18 位栅格（A组/B组/自选）
7. **交付：3 版方向并排在一个画布对比**
8. 强调/避免：（暂无）

## 当前 Claude Design prompt（v2，可直接粘贴）
```
我在为《星际争霸2》合作任务电竞赛事「集结杯」设计比赛工具 UI（抽签+对战记分）。B站主播「土豆」主办、与社区 MOD「CM」合作，需 CM×集结杯 联名视觉。注意：参考截图里出现的「回归杯」是旧名，一律改用「集结杯」。

【产出 3 版方向，并排在同一个画布上对比】三版共用「深藏青 navy + 金属金 + 银」基调，按"质感"拉开差异：
- 方向A：精修金属质感（写实厚重金属、高光）
- 方向B：极简 HUD（科幻线框/大留白、最克制）
- 方向C：重描边漫画感（厚黑描边、扁平饱和）
每版给：下面 4 屏 mock + 一页 配色/字体 规范 + 一个 CM×集结杯 联名 logo 概念。

【联名 logo】用我附的 CM 金属 dM/CM 字标，锁定方式 = CM 字标 + 赛事名「集结杯」左右并排，中间加一条分隔线。

【整屏背景】Cocos 里是一张整图：星际2 战场实景照、做暗化处理（压暗保证文字可读），背景本身不含任何文字（文字另用 Label 叠）。

【4 个界面】（横屏 16:9，中文 UI）
1. 首页：标题「集结杯」+ 主办/B站信息 + 「比赛模式」按钮弹性栅格（先放 6 个核心示意：8因子/10因子/12因子/极难/随机/单指挥官，栅格要能容纳 6–9 个）+ 选手名输入框。
2. 选择面板：一排「因子」图标 + 分组的「指挥官」头像（A组/B组/自选）——用 6 个真头像示意 + 占位补满到 18 位的栅格；玩家把因子/指挥官拖到 3 场槽位；要自动换行、不溢出。
3. 对战页：3 场竖直对齐，每场含 地图缩略图 + 指挥官头像 + 若干因子图标 + 胜利/带奖励/失败 按钮。
4. 结算页：本局战绩（获胜 X 场）。

【约束】最终在 Cocos Creator 用 节点+图片+文字 重做（不是网页）：基准分辨率 1280×720；清晰栅格 + 明确字号层级（标题/正文/按钮）；文字框留足空间避免溢出；≤3 主色、克制、留白。先给静态视觉与布局，拖拽手感之后做。

【参考图】已附：现状 5 屏截图、CM 金属 logo、CM 公告图、游戏图标样例。

【交付】3 版方向并排在一个画布对比；每版 4 屏 + 配色色值 + 字号层级 + logo 概念。定稿后我用 Handoff 导出规范，工程在 Cocos 落地。
```

> 参考图包：`~/Downloads/集结杯设计参考图/`（拖进 Claude Design）。
