
export default class JJLog {
    public static writeLog(arr: any[]): void {
        // var fs:FileStream = new FileStream();
        // fs.open(File.userDirectory.resolvePath("集结杯比赛记录.txt"),FileMode.APPEND);
        // for(var i:int = 0;i<arr.length;i++){
        // 	fs.writeMultiByte(arr[i],"UTF-8");
        // 	if(i<arr.length-1){
        // 		fs.writeMultiByte("\t","UTF-8");
        // 	}else{
        // 		fs.writeMultiByte("\r\n","UTF-8");
        // 	}
        // }
        // fs.close();
    }

    public static writeMatchLog(arr: any[]): void {
        // var fs:FileStream = new FileStream();
        // fs.open(File.userDirectory.resolvePath("集结杯指挥官胜负场统计.txt"),FileMode.APPEND);
        // for(var i:int = 0;i<arr.length;i++){
        // 	fs.writeMultiByte(arr[i],"UTF-8");
        // 	if(i<arr.length-1){
        // 		fs.writeMultiByte("\t","UTF-8");
        // 	}else{
        // 		fs.writeMultiByte("\r\n","UTF-8");
        // 	}
        // }
        // fs.close();
    }
}