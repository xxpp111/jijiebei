# 集结杯 · 近期进展（generation4 correction 时点）

> 修订时间 2026-08-23 15:35:00 · 基线 HEAD `d752ce9` · authoring mode=`digest-active` · life=`flow`。
> 本 revision 只订正 `r20260823-111300` 的时点表述；旧 revision 保持原字节，未做 live 验证的事项继续标为 `CANNOT_VERIFY`。

## 1. GC 删除与目录恢复的准确口径

- GC 删除目标中与 `test-x` 相关的只有文件 `test-x/x.md`；不能写成整个 `test-x/` 被删除。
- `test-x/` 曾因 manifest 外的空父目录清理而消失，随后已由 Hub 在 host-local 回建；当前目录存在且为空。
- `docs/superpowers/plans/` 与 `docs/superpowers/specs/` 同样已回建且为空。恢复空目录不撤销已获批准的三个文件删除，也不产生新的 GC 结论。

## 2. generation3 已完成，但 audit 不是全绿声明

- `.harness-pro-alioth-v2-r3` 的 strict 两个 phase 均为 `completed`，两个 gate 均为 `PASS`；这是 Harness gate 结论，不等于提交、CI、部署或 owner approval。
- r3 终态账本为 `log=90`、`lifecycle=94`，wiki-governance 测试块总数为 22 且当轮验证通过。
- r3 最终 audit 的 `overall=partial_compliance`，不得升级为 full audit PASS；其中 `post_review_npm_test` 仍是 `skipped/not-run`（`no recognized test runner`）。

## 3. 历史 partial 与未验证边界

- r2 历史 partial 五项继续分别保留：host reviewer `0/20` real use 且 20 次 fallback；post-review npm 为 `skipped/not-run`；mutation check 为 `matches=false`；invalid direct lifecycle 计数为 20；migration live 为 `CANNOT_VERIFY`。这些历史字段不能由后续局部门禁反推为 PASS。
- 迁服 live 状态本轮未核验，仍为 `CANNOT_VERIFY`；仓内 migration 检查或本地测试不能替代 live 迁服证据。
- frozen 历史中存在 pre-existing secret literal risk（至少涉及 `projectplan` 与 archive），owner 已将轮换/清理延期。本 correction 不回显字面量，也不扩大到 archive redaction；是否有本轮新增命中只以终态 changed-content 与实际 env-value 扫描结果为准。

## 4. 本 revision 的结论边界

本 revision 记录的是 generation3 已结事实与 generation4 correction 的输入基线，不写 generation4/r4b 尚未完成的 Phase 2 gate、stop 或 audit verdict。最终 closeout 必须继续把 deterministic checks、5-reviewer gate、Harness audit、提交/CI/部署和 owner approval 分开陈述。
