# 集结杯 · 近期进展（digest 草稿）

> 蒸馏日期 2026-08-23 · 基线 HEAD d752ce9 · 状态 flow。
> 事实只取自 git log 与仓内文档；未做 live 验证的项明确标注。

## 1. 近 8 个 commit 人话版（新 → 旧）

| commit | 干了什么 |
|---|---|
| `d752ce9` docs(governance) | **文档治理 v1 收口**：活文档建立唯一 home + 漂移机器门（`docs-drift-check` 扩到七项检查），front matter 九字段齐备 |
| `1df08f5` docs | 双打规则 canonical 路径受控 rename：`规则一-官突加n-细则v1.md` → `doubles-guantu-plus-n.md`，旧路径留兼容指针防断链 |
| `7c5806c` fix(deploy) | systemd unit 监听地址对齐现网 `0.0.0.0:8090`；部署文档统一 Docker nginx 经 `172.17.0.1:8090` 访问宿主 backend 的拓扑说明 |
| `1dfafc1` docs(jjb) | testing.md 登记 5 个新 e2e（计数校准 26 e2e / 8 flows / 13 vitest 文件）+ projectplan 真相校准记录 |
| `4a3ce8e` feat(jjb) | **土豆 AI 彩蛋上线**：`potato_ai_bias` 加权直达旧世机械团（单场 ≈76.32%）+ Select/Battle/OBS 三屏「土豆的老朋友」标签（canonical 知识页见 `docs/potato-ai-bias.md`） |
| `cf4709c` fix(jjb) | **Round S 安全止血**：account role 字段级不可变（堵自提权）+ practice 归属 fail-closed（堵伪造他人归属），两条隔离 PB 负测在库 |
| `5586082` feat(jjb) | select 交互增强：reroll 旧因子消失、指挥官候选区空出/移动/拖回、点击与拖拽区分 |
| `dc3a874` merge | `jjb-select-interact` 合入 `jjb-platform` |

## 2. 当前分支状态

- **v1 已推未 merge**：`docs/doc-governance-v1-20260822` 已推 origin（tip = d752ce9），领先 `main` 116 个 commit 未 merge（merge-base = `ed161d6`）。
- **本分支 v2 堆叠**：`docs/alioth-governance-v2-20260823` 与 v1 同 HEAD（d752ce9），v2 工作全部在工作区**未提交**：
  - `docs/wiki/` 账本（log/lifecycle/README + 首账 `jijiebei/alioth-governance@r20260823-145244`）；
  - `web/scripts/wiki-governance.mjs` + 测试（register/check/query/build-context CLI）；
  - 本批中文蒸馏（potato-ai-bias 知识页 + 四份 wiki 草稿 + 原页短指针）。
- 工作区另有历史遗留未提交项（非本轮产生）：`test-x/`、`backend/pocketbase-linux-amd64`（构建产物）、`.gitignore` / `web/package.json` / `web/scripts/README.md` / `docs-drift-check.mjs` 的修改。

## 3. 迁服待办（据仓内文档收拢）

> 背景：文档治理 v1 的目标之一是「迁服前把当前事实在哪、什么只是历史、如何机器验证固化」（projectplan 2026-08-22 段）。以下为与迁服相关的未竟项：

| 项 | 状态 | 源锚点 |
|---|---|---|
| 系数表定稿（天梯上线前必须） | 占位可跑，待 yb/土豆拍板（open-decisions D1） | `docs/deployment.md` §10.4 |
| Litestream 容灾 endpoint/bucket | 模板占位，待 yb 拍板对象存储（open-decisions D6） | `docs/deployment.md` §10.2 |
| CF Pages 公网正解 | 方向已定未施工（engineering-backlog B1） | `docs/deployment.md` §10.1 |
| 换机本机配置补回 | `config/` 被 gitignore（host-reviewer-allowlist 等），换机需手工补 | `projectplan.md` 历史段（CANNOT_VERIFY：当前是否仍缺未 live 核对） |
| 部署前迁移核对 | **已入流程**：`backend/scripts/check-migrations.mjs` + jjb-deploy skill 前置步骤 | `docs/deployment.md` §11 |
| 现网部署版本 | 文档记录 devbox 部署 `b0076d5`（落后于 HEAD）——CANNOT_VERIFY：当前 live 版本未在本轮核对 | `projectplan.md` 历史段 |
