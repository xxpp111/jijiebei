<!--lm jijiebei/alioth-gc@r20260823-111500-->
# Alioth v2 GC（manifest v3）结算报告

- **revision**: `jijiebei/alioth-gc@r20260823-111500`
- **type**: report ｜ **life**: flow（report-validating 语义；A3 mode 矩阵交付前 CANNOT_VERIFY，以 sub-hub 注册为准）
- **author**: spoke-b3（jjb-alioth-v2-spoke-b3，gen3 B3-7）
- **date**: 2026-08-23
- **关联结算件**: [gc-gc-alioth-v2-20260823-065156-v3.attempt2-settlement.json](../../_receipts/gc-gc-alioth-v2-20260823-065156-v3.attempt2-settlement.json)

## 1. 背景

Alioth/玉衡 doc-governance v2 本轮 GC 由 Hub 编排、spoke 执行，对象为 gen2 遗留的 3 个陈旧文件与 6 个 zellij docgov 容器。执行经历两次 attempt：

| attempt | 结果 | 根因 |
|---|---|---|
| attempt1 | **INVALID**（全量回滚） | V-6 check-red-then-apply（先制造漂移再跑 check）+ V-7 receipt 覆盖事故 |
| attempt2 | **已结算**（本报告） | reply-5 逐字授权 + apply 前原位复验全绿后执行 |

## 2. 四件权威 artifact（缺一不可）

| # | artifact | 路径 | 角色 |
|---|---|---|---|
| 1 | manifest v3 | `.harness_local/alioth-v2/gc-manifest.json` | 9 对象固定清单（3 safe-delete/distill-then-remove + 6 safe-close），sha256 `40e6cbd4…f9b41` |
| 2 | attempt1-invalid receipt | `docs/wiki/_receipts/gc-gc-alioth-v2-20260823-065156-v3.attempt1-invalid.json` | 失败历史件，保留作教训，禁止覆盖 |
| 3 | attempt2 raw receipt | `docs/wiki/_receipts/gc-gc-alioth-v2-20260823-065156-v3.attempt2.json` | apply 原始记录（即时计数 3/3/3 + 后验），一字未改 |
| 4 | **settlement receipt** | `docs/wiki/_receipts/gc-gc-alioth-v2-20260823-065156-v3.attempt2-settlement.json` | 本报告的结算依据：授权证据、时间戳机械证明、对象级终态、manifest 外 rmdir 真相 |

> 四份文件互为证据链：manifest 定范围 → attempt1 留失败教训 → attempt2 raw 留执行原貌 → settlement 做终态裁决。任何后续引用以 settlement 为准，raw 不追改。

## 3. 终态摘要（详见 settlement receipt）

- **授权**：reply-5（全文 SHA256 `6e64a589…598b`，spoke-b3 复算一致），approval `10:14:11.800Z` 早于 apply `10:15:07.775Z` 共 55.975 秒，approval-before-apply 机械成立。
- **对象级终态**：deleted=3（test-x/x.md、superpowers plans/specs 两稿——后者原稿已 verbatim 归档 `potato-ai-bias@r20260823-094100/094101`）＋ closed=6（docgov 六容器），合计 9/9。
- **竞态说明**：apply 即时计数 3/3/3 中 3 个 STILL_LISTED 系 `--force` 后立即 list-sessions 的竞态误报；后验 `zellij list-sessions | grep docgov` 零命中，6/6 全消失。
- **manifest 外 rmdir**：apply-gc.mjs:92-95 额外 rmdir 了 4 个空父目录（test-x、docs/superpowers/plans、docs/superpowers/specs、docs/superpowers），与 attempt2 raw 的 `out_of_manifest_touched=false` 自相矛盾——以「happened=true」为准。**Hub 已回滚重建为空目录**，spoke-b3 于 11:12Z 幂等复核存在且为空、git 无感知；保持空目录，不再删除、不放文件。
- **factor-pick normal close**：`jijiebei-doubao-pick` lane 于 factor-pick artifact 落账后由 Hub 计划内 normal close，**不属于** GC 计数。

## 4. 边界与纪律

- 本报告只引用既有 artifact，不重述 receipt 全文；机器消费以 settlement receipt 为准。
- 两个 attempt receipt 与本报告三者各自独立，禁止互相覆盖（V-7 教训）。
- 本 revision 的落账（register）由 sub-hub 经 registrar CLI 执行；spoke-b3 不写 log/lifecycle。

## 5. r2 audit partial 与最终 claim 归因

以下五项是最终 claim 必须逐项保留的事实；它们是审计/验证边界，不得人工升级：

| 字段 | 原值与归因 |
|---|---|
| host fallback | r2 `total=20`、`host_completed=0`、`host_failed=20`、`fallback_to_direct=20`、`host_real_use_ratio=0/20`；真实 host spawn 未获授权，20 次均走 direct-dubhe fallback。 |
| npm | r2 `post_review_npm_test.status=skipped`、`source=not-run`、`reason=no recognized test runner`；这是 Harness 未运行该门，不得写成已执行。 |
| mutation | r2 `post_review_mutation_check.matches=false`；两个 git-status hash 相等不能把工具判定反推为 true。 |
| direct lifecycle | r2 `invalid_direct_dubhe_lifecycle_count=20`；fallback 调用成功不等于 host 生命周期合规。 |
| migration live | `CANNOT_VERIFY`；gen3 的局部 CLI 修正证据不能替代迁服 live 验证，不得升级。 |

完整展开稿保存在 `.harness_local/alioth-v2/spoke-b3/final-claim-audit-attribution.md`；本节是供 repo-visible reviewer 独立核对的等值镜像。
