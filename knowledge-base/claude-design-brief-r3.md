# 集结杯 · Claude Design 提交稿 R3（模式 × BP 设计项）

> 2026-06-17。承接 `bp-impl-spec.md`（BP 框架决策①-⑧ + Q1-Q6 拍板 + §4 五交互 brief）与 `rules-decisions.md`。
> 本稿是今天的**增量与对齐**：模式归类重组、BP 每模式独立开关、随机/自选叠加层落地状态。
> 红线 jijie2/jjdata 不在本稿（抽取内核零改，配置驱动见 bp-impl-spec）。本稿聚焦**要交给 Claude Design 出 UI 的设计项**。

## 一、模式归类总表（最终）

三大类 + 每模式 BP 默认态 + 随机/自选叠加层状态：

| 大类 | 模式 | 因子 | BP 默认 | BP 可调 | 随机叠加 | 自选叠加 |
|---|---|---|---|---|---|---|
| **单刷·标准** | 8 因子 | base 8 | **开** | ✓ | 随机填充按钮 ✅已实现 | 拖拽手选+自选池 ✅已实现 |
| **单刷·标准** | 10 因子 | base 10 | **开** | ✓ | ✅ | ✅ |
| **单刷·标准** | 拯救 | 固定 7 人 | **关** | ✓ | ✅ | 固定 7 人，无自选 |
| **单刷·极难** | 极难（=12 因子） | base 12 | **关** | ✓（可手动开） | ✅ | ✅ |
| **双打挑战** | 官突 plus | 2 官突+3 随机 | 可配 | ✓ | 第四步细抠 | 第四步细抠 |
| **双打挑战** | 非酋 | 3 固定 | 可配 | ✓ | 第四步细抠 | 第四步细抠 |

口径要点：
- **12 因子 = 极难模式**（赛制方拍板）：UI 已合并，删「极难②」，原 std12 显示「极难模式」。
- **BP = 每模式独立开关**（覆盖旧 bp-impl-spec Q3「全局 all」）：每模式赛前可设默认开/关，主播可手动调。
- **极难 BP 默认关但可手动开**（不锁死）。

## 二、BP 框架要点（承接已拍板，Claude Design 出 UI 据此）

- **BP 形态**：ban 1 因子 _或_ 自选 1 指挥官（二选一，主形态先上）；框架可配「都给」/多 ban。
- **点金**：赛前固定金框名单 + 选手每场点金；软件作用 = 分值 ×2（仅视觉显示，**不入排名**，主计分=场数）；抽取概率不变。点金 3 态：未金 / 赛前金框 / 运行时点金。
- **额外难度**：开关 + n（默认 2），开则每场 base+n 因子（末场 +n 分摊）。
- **池层**（后端配置驱动，非 UI）：因子全量+自定义−排除单因子；指挥官 A11/B7（ban 凯拉克斯/凯瑞甘）、德哈卡/泰凯斯权重 0.25；拯救固定 7 人含凯瑞甘不吃 ban。

## 三、叠加层落地状态

- **随机**：select 面板「随机填充」按钮 ✅ 已实现（一键随机填满当前局指挥官+因子，可再手调；不直接进对战）。
- **自选**：拖拽手选 + 自选指挥官池（selfShow 门控：拯救/随机/极难①/非酋 无自选区）✅ 已实现。
- **已选反馈**：已选因子/指挥官打对勾 ✓ ✅ 已实现。

## 四、Claude Design 设计项清单（要出的 UI）

承接 `bp-impl-spec.md §4` 的 5 交互（沿用 R1 选择面板设计语言 + 6 主题 token，在开局流程**前**插 BP/点金/难度层，不重新发明视觉；各出 metal-dark/sc2-dark/minimal-light 三屏 + 状态矩阵；1280×720）：

1. **BP 面板**（二选一：ban 1 因子 / 自选 1 指挥官）
2. **Ban 因子**（揉横排，选手点 ban，剩下进手选）
3. **自选指挥官**（扩展现有自选池交互）
4. **点金**（赛前固定 + 每场点金，3 态 + ×2 角标）
5. **额外难度开关**（toggle + n 步进，默认 2）

**今天新增设计项（本稿重点）：**

6. **每模式 BP 开关配置面**：模式选择/赛前配置处，每模式一个 BP 开关，体现默认态（8因子/10因子=开、拯救/极难=关、双打=可配），主播可手动切。需设计：开关组件 + 默认态视觉 + 极难"默认关可开"的非锁死表达。

## 五、与旧文档更新点（提交前对齐）

| 项 | 旧 bp-impl-spec | 今天更新 |
|---|---|---|
| BP 适用范围 | Q3：全局 all 不分模式 | **每模式独立开关** |
| 极难 BP | （未单列） | 默认关，可手动开（不锁死） |
| 模式列表 | std8/10/12 + 极难①②… | **12 因子=极难**（合并，删极难②） |
| 随机 | 独立 suiji 模式 | 叠加层「随机填充」按钮（已实现） |

> 逐模式完整规则细则见 `design/v4-input-r2/r3-6modes-rules-detail.md`；开放问题清单见 `r3-6modes-open-questions.md`。

## 六、第四步逐模式细抠 · 拍板记录（2026-06-17）

**P0 核心：**
- **O1 极难判定**：极难=12因子 内部纯 `mfc==4`(std12) 判定，**废弃 modeIsVeryHard①②** 旧路径（与「删极难②」一致）。
- **O2 非酋结构**：非酋**确实是双打 2v2 玩法**，归「双打挑战」正确；但当前代码是单刷实现（mfc=1/A·B 分组），**需重写为双打独立骨架**——本轮设计先行，工程下轮排期。
- **O3 官突plus**：本轮**只出 UI 设计稿**，工程入口（startDoublesSession + doubles 数据模型适配）下轮单独排期。

**P1：**
- **O4/O5 BP 自选门控**：**破例支持自选**——拯救/非酋等 selfShow=false（无叠加层自选区）的模式，BP 开启时也支持「自选 1 指挥官」分支。⚠️ **Claude Design 需设计一条独立的 BP 自选指挥官 UI 路径**，不受 selfShow 叠加层门控限制（两套「自选」语义解耦：叠加层自选区 vs BP 二选一的自选分支）。
- **O6 降权**：非酋归双打后按双打口径（双打不吃单刷降权）；标准 8/10/12 维持降权 0.25。

**P2（按推荐默认，文档化/低风险）：** O7 8因子 0.3 删同化体=保留+注释；O8 smallRate=0.9 死注释=删；O9 非酋删人/3因子=随双打重写处理；O10 官突 mutators/maps=移 DOUBLES_CONFIG 配置化；O11 拯救 7人→3场映射=明确分配算法补真相表；O12 随机因子数7=查配置；O13 BP互斥=R3 已覆盖（互斥=1）。

**矩阵更新（据拍板）：**
- 极难 = std12（mfc==4），modeIsVeryHard①② 下线。
- **双打挑战（官突plus + 非酋）= 本轮设计先行、工程下轮**；非酋从单刷实现迁双打骨架。
- 全模式 BP 自选分支均支持（含 selfShow=false 模式），Claude Design 第 6 设计项「每模式 BP 开关配置面」+ 第 1 项「BP 面板」需含无自选区模式的自选路径。

## 七、新增需求（2026-06-18）：每场难度分 + 难度总分

用户反馈：难度总分（`difficultyTotal`，GLM 已做、**保留**）之外，还要**每一场（3 场）各自的难度分**；**不要「难度组」**（按因子组/type 分类的难度视图，去掉，只要数值化的「总分 + 每场分」）。

- **逻辑（GLM spoke 实现）**：新增 `matchDifficulty(slot:0|1|2)` = 该场锁定因子（`lockFactorList[slot]`）+ 该场手选因子（`selectedFactorList[facFlatIdx(slot,k)]`，k 遍历 `manualSlots(slot)`）的 `factorScore` 求和（点金 ×2）。`difficultyTotal` = Σ `matchDifficulty`（等价）。透出 `data-match-difficulty-{slot}`。复用现有 factorScore 白名单（已复核），不改 jijie2。
- **设计项 C7 · 每场难度分显示（Claude Design 出 UI）**：在 select 每场 slot-head、battle MatchRow、result rcard 显示该场难度分（数值），与「难度总分」（topbar/meta 行）层级区分。各 3 主题三屏 + 数值态（0 / 低 / 高）。**不做**「难度组」分类视图。
